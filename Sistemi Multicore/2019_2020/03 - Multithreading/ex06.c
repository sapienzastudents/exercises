/*
- Si scriva un programma che definisce due funzioni:

- char * leggiStringa() –legge una stringa in input memorizzandola
  dinamicamente e ritorna un puntatore a questa stringa

- void concatena(char *p1, char *p2) – prende in input due parametri che
  sono due puntatori a due rispettive stringhe. La funzione deve
  concatenare in p1, le due stringhe puntate rispettivamente da p1 e p2

- Infine si dichiari la funzione main che per prima cosa permette di inserire
  due stringhe all’utente (richiamando la funzione leggiStringa), le
  concatena (richiamando la funzione concatena) ed infine stampa la
  stringa concatenata.
*/

#include <stdio.h>
#include <stdlib.h>
#include <pthread.h>

int main(){
    return 0;
}