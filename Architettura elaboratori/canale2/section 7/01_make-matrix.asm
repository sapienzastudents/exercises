# Implementare un programma che definiti R (numero di righe) e C (numero di colonne) rispettivamente in $t0 e $t1, 
# stampa in maniera leggibile e conforme alla struttura tabellare una matrice M(RxC) definita in memoria. 